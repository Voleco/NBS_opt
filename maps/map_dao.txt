for i in dao/b*.map; do printf "$i\n"; ../bin/release/bidirectional -nbs $i ../scenarios/$i.scen | grep "^A" | awk 'BEGIN {} {a += $2; b += $4; s++ } END { printf("s: %d A*: %d NBS: %d\n",s, a, b) }'; done